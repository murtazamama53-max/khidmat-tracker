Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.0.7
python-dotenv==1.0.1
google-auth-oauthlib==1.2.1
google-api-python-client==2.140.0
reportlab==4.2.2
cryptography==43.0.1
Flask-WTF==1.3.0
pytest==8.3.3
pypdf==5.0.1
# Production database + WSGI server. Not needed for local SQLite dev, but
# harmless to have installed everywhere so `pip install -r requirements.txt`
# is the single command that works for both local dev and deployment.
psycopg2-binary==2.9.12
gunicorn==26.1.0
